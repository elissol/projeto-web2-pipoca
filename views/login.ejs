<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Login | Pipoca Gourmet</title>

    <style>
        body {
            background: pink;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .login-container {
            background: white;
            padding: 30px;
            border-radius: 12px;
            width: 350px;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.2);
            text-align: center;
        }

        h2 {
            color: #d63384;
            margin-bottom: 20px;
        }

        input {
            display: block;
            width: 90%;
            margin: 12px auto;
            padding: 12px;
            border: 2px solid pink;
            border-radius: 8px;
        }

        button {
            width: 95%;
            padding: 12px;
            border: none;
            background: #d63384;
            color: white;
            font-size: 16px;
            border-radius: 8px;
            cursor: pointer;
        }

        button:hover {
            background: #b62a6d;
        }

        .error {
            color: red;
            font-size: 15px;
            margin-top: 5px;
        }

        .footer {
            font-size: 13px;
            margin-top: 15px;
            color: gray;
        }
    </style>
</head>
<body>

    <div class="login-container">
        <h2>Acesso Administrativo</h2>

        <form action="/login" method="POST">
    <input type="email" name="email" placeholder="Digite seu e-mail" required>
    <input type="password" name="senha" placeholder="Digite sua senha" required>
    <button type="submit">Entrar</button>

    <% if(erro) { %>
        <p style="color:red;"><%= erro %></p>
    <% } %>
</form>


        <% if (erro) { %>
            <p class="error"><%= erro %></p>
        <% } %>

        <p class="footer">Sistema da Pipoca Gourmet 🍿</p>
    </div>

</body>
</html>
