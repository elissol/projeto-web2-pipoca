<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerenciar Doces - Admin</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #2c3e50, #34495e);
            min-height: 100vh;
            color: #ecf0f1;
        }

        .admin-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        .admin-header {
            text-align: center;
            margin-bottom: 30px;
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 15px;
        }

        .admin-header h1 {
            color: #fff;
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        .admin-nav {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin: 20px 0;
        }

        .admin-nav a {
            background: linear-gradient(45deg, #e74c3c, #c0392b);
            color: white;
            padding: 10px 20px;
            border-radius: 20px;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .admin-nav a:hover {
            transform: translateY(-2px);
        }

        .back-btn {
            background: #7f8c8d;
            color: white;
            padding: 10px 20px;
            border-radius: 20px;
            text-decoration: none;
            display: inline-block;
            margin-bottom: 20px;
        }

        .categoria {
            margin-bottom: 40px;
        }

        .categoria h2 {
            color: #e74c3c;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e74c3c;
        }

        .doces-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
        }

        .doce-card {
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .doce-header {
            display: flex;
            justify-content: between;
            align-items: center;
            margin-bottom: 15px;
        }

        .doce-tipo {
            background: #e74c3c;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 0.8em;
            font-weight: bold;
        }

        .doce-info {
            margin-bottom: 15px;
        }

        .doce-info div {
            margin-bottom: 5px;
        }

        .doce-actions {
            display: flex;
            gap: 10px;
        }

        .btn {
            padding: 8px 15px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 0.9em;
            transition: all 0.3s ease;
            flex: 1;
            text-align: center;
        }

        .btn-edit {
            background: #3498db;
            color: white;
        }

        .btn-delete {
            background: #e74c3c;
            color: white;
        }

        .btn:hover {
            opacity: 0.8;
        }

        .favorito {
            color: #e74c3c;
        }

        .empty-state {
            text-align: center;
            padding: 40px;
            color: #95a5a6;
            grid-column: 1 / -1;
        }

        .empty-state i {
            font-size: 3em;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <a href="/admin" class="back-btn"><i class="fas fa-arrow-left"></i>Voltar ao Dashboard</a>
        
        <div class="admin-header">
            <h1><i class="fas fa-cookie-bite"></i>Gerenciar Doces</h1>
            <p>Edite ou exclua doces existentes</p>
            
            <div class="admin-nav">
                <a href="/admin"><i class="fas fa-chart-bar"></i> Dashboard</a>
                <a href="/admin/pedidos"><i class="fas fa-clipboard-list"></i> Todos os Pedidos</a>
                <a href="/admin/doce/novo"><i class="fas fa-plus"></i> Novo Doce</a>
            </div>
        </div>

        <% if (doces) { %>
            <% if (doces.brigadeiros && doces.brigadeiros.length > 0) { %>
            <div class="categoria">
                <h2><i class="fas fa-star"></i> Brigadeiros (<%= doces.brigadeiros.length %>)</h2>
                <div class="doces-grid">
                    <% doces.brigadeiros.forEach(doce => { %>
                    <div class="doce-card">
                        <div class="doce-header">
                            <span class="doce-tipo">Brigadeiro</span>
                            <% if (doce.favorito) { %>
                                <i class="fas fa-heart favorito"></i>
                            <% } %>
                        </div>
                        <div class="doce-info">
                            <div><strong>Cliente:</strong> <%= doce.cliente %></div>
                            <div><strong>Sabor:</strong> <%= doce.sabor %></div>
                            <div><strong>Quantidade:</strong> <%= doce.quantidade %> unidades</div>
                            <div><strong>Valor:</strong> R$ <%= parseFloat(doce.valor_total).toFixed(2) %></div>
                            <div><strong>Data:</strong> <%= new Date(doce.created_at).toLocaleDateString('pt-BR') %></div>
                        </div>
                        <div class="doce-actions">
                            <a href="/admin/doce/editar/brigadeiro/<%= doce.id %>" class="btn btn-edit">
                                <i class="fas fa-edit"></i> Editar
                            </a>
                            <a href="/admin/doce/excluir/brigadeiro/<%= doce.id %>" class="btn btn-delete" onclick="return confirm('Tem certeza que deseja excluir este brigadeiro?')">
                                <i class="fas fa-trash"></i> Excluir
                            </a>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
            <% } %>

            <% if (doces.brownies && doces.brownies.length > 0) { %>
            <div class="categoria">
                <h2><i class="fas fa-birthday-cake"></i> Brownies (<%= doces.brownies.length %>)</h2>
                <div class="doces-grid">
                    <% doces.brownies.forEach(doce => { %>
                    <div class="doce-card">
                        <div class="doce-header">
                            <span class="doce-tipo">Brownie</span>
                            <% if (doce.favorito) { %>
                                <i class="fas fa-heart favorito"></i>
                            <% } %>
                        </div>
                        <div class="doce-info">
                            <div><strong>Cliente:</strong> <%= doce.cliente %></div>
                            <div><strong>Sabor:</strong> <%= doce.sabor %></div>
                            <div><strong>Tamanho:</strong> <%= doce.tamanho %></div>
                            <div><strong>Quantidade:</strong> <%= doce.quantidade %> un</div>
                            <div><strong>Valor:</strong> R$ <%= parseFloat(doce.valor_total).toFixed(2) %></div>
                            <div><strong>Data:</strong> <%= new Date(doce.created_at).toLocaleDateString('pt-BR') %></div>
                        </div>
                        <div class="doce-actions">
                            <a href="/admin/doce/editar/brownie/<%= doce.id %>" class="btn btn-edit">
                                <i class="fas fa-edit"></i> Editar
                            </a>
                            <a href="/admin/doce/excluir/brownie/<%= doce.id %>" class="btn btn-delete" onclick="return confirm('Tem certeza que deseja excluir este brownie?')">
                                <i class="fas fa-trash"></i> Excluir
                            </a>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
            <% } %>

            <% if (doces.pipocas && doces.pipocas.length > 0) { %>
            <div class="categoria">
                <h2><i class="fas fa-popcorn"></i> Pipocas (<%= doces.pipocas.length %>)</h2>
                <div class="doces-grid">
                    <% doces.pipocas.forEach(doce => { %>
                    <div class="doce-card">
                        <div class="doce-header">
                            <span class="doce-tipo">Pipoca</span>
                            <% if (doce.favorito) { %>
                                <i class="fas fa-heart favorito"></i>
                            <% } %>
                        </div>
                        <div class="doce-info">
                            <div><strong>Cliente:</strong> <%= doce.cliente %></div>
                            <div><strong>Sabor:</strong> <%= doce.sabor %></div>
                            <div><strong>Peso:</strong> <%= doce.peso %></div>
                            <div><strong>Quantidade:</strong> <%= doce.quantidade %> un</div>
                            <div><strong>Valor:</strong> R$ <%= parseFloat(doce.valor_total).toFixed(2) %></div>
                            <div><strong>Data:</strong> <%= new Date(doce.created_at).toLocaleDateString('pt-BR') %></div>
                        </div>
                        <div class="doce-actions">
                            <a href="/admin/doce/editar/pipoca/<%= doce.id %>" class="btn btn-edit">
                                <i class="fas fa-edit"></i> Editar
                            </a>
                            <a href="/admin/doce/excluir/pipoca/<%= doce.id %>" class="btn btn-delete" onclick="return confirm('Tem certeza que deseja excluir esta pipoca?')">
                                <i class="fas fa-trash"></i> Excluir
                            </a>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
            <% } %>
        <% } %>

        <% if ((!doces.brigadeiros || doces.brigadeiros.length === 0) && 
              (!doces.brownies || doces.brownies.length === 0) && 
              (!doces.pipocas || doces.pipocas.length === 0)) { %>
            <div class="empty-state">
                <i class="fas fa-cookie"></i>
                <h3>Nenhum doce encontrado</h3>
                <p>Comece adicionando alguns doces ao sistema.</p>
                <a href="/admin/doce/novo" class="btn" style="background: #27ae60; color: white; padding: 10px 20px; border-radius: 20px; text-decoration: none; margin-top: 20px;">
                    <i class="fas fa-plus"></i> Adicionar Primeiro Doce
                </a>
            </div>
        <% } %>
    </div>
</body>
</html>